// iOS Shortcut to get the last baby eat event
{
  "actions": [
    {
      "type": "web.request",
      "parameters": {
        "url": "http://YOUR_SERVER_IP:3024/baby-eat/last",
        "method": "GET"
      }
    },
    {
      "type": "dictionary.get",
      "parameters": {
        "dictionary": "$.result",
        "key": "timestamp"
      }
    },
    {
      "type": "notification",
      "parameters": {
        "title": "Last Feeding",
        "body": "$.result"
      }
    }
  ],
  "name": "Get Last Baby Eat"
}

// To use:
// 1. Replace YOUR_SERVER_IP with your actual server IP
// 2. Save to Files app
// 3. Open and tap "Add Shortcut" when prompted 